for(let i=0; i<createSpecs.length;i++) {

}

if(createSpecs.length > 0) {
  for(let i=0; i<createSpecs[0].v.length;i++) {

    if(createSpecs.length > 1) {
      for(let j=0; j<createSpecs[1].v.length;j++) {
        if(createSpecs.length > 2) {
          for(let m=0; m<createSpecs[2].v.length;m++) {
            
          }
        }
      }
    }
  }
}